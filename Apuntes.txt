

The Symfony CLI v4.18.1 was installed successfully!

Use it as a local file:
  /home/saragar/.symfony/bin/symfony

Or add the following line to your shell configuration file:
  export PATH="$HOME/.symfony/bin:$PATH"

Or install it globally on your system:
  mv /home/saragar/.symfony/bin/symfony /usr/local/bin/symfony

Then start a new shell and run 'symfony'

 symfony new my_project_name --version=4.4 --full
 composer create-project symfony/website-skeleton:"^4.4" my_project_name


symfony server:start

php bin/console about
composer require --dev debug
symfony check:security


# Run "composer dump-env prod" to compile .env files for production use (requires symfony/flex >=1.2).


config/
    Contains… configuration!. You will configure routes, services and packages.
src/
    All your PHP code lives here.
templates/
    All your Twig templates live here. 


checkear errores en plantillas 

 php bin/console lint:twig


 php bin/console lint:twig templates/email/


MAKER BUNDLE
https://symfony.com/doc/current/bundles/SymfonyMakerBundle/index.html
 composer require symfony/maker-bundle --dev
 php bin/console list make
 make:command            Creates a new console command class
 make:controller         Creates a new controller class
 make:entity             Creates a new Doctrine entity class

 [...]

 make:validator          Creates a new validator and constraint class
 make:voter              Creates a new security voter class

